name: emoji_feedback
description: An mood feedback component which could be used for rating the user's mood.
version: 0.1.0
author: fedeoo <fedeoo.zf@gmail.com>
homepage: https://github.com/fedeoo/emoji_feedback/

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
   assets:
    - assets/
