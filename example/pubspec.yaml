name: example
description: An example for emoji_feedback.
version: 0.0.1
author: fedeoo <fedeoo.zf@gmail.com>

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  emoji_feedback:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter

